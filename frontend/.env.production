# Production — used by: vite build (Firebase deploys the built dist folder)
# This URL is the Cloud Run service URL. No proxy in production; the browser
# calls Cloud Run directly. The backend CORS config must list the Firebase domain.
VITE_API_BASE_URL=https://electiq-backend-109778663813.asia-south1.run.app/api/v1
